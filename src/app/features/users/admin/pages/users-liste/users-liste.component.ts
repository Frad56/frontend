import { Component, inject } from '@angular/core';

@Component({
  selector: 'app-users-liste',
  standalone: true,
  imports: [],
  templateUrl: './users-liste.component.html',
  styleUrl: './users-liste.component.css'
})
export class UsersListeComponent {

  //private userServcie = inject(User);
}
